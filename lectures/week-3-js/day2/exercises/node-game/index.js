// look at the documentation for readline
// Can you use it to help you make a simple
// number guessing game?
// Generate a random number and then give the user
// three chances to guess it. 
const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
});
